# OR Project Dependencies
# 用于 EV Sharing System Optimization 项目

# 核心计算
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# 优化求解器
gurobipy>=9.0.0  # Gurobi Python API (需要已安装 Gurobi 并激活 License)

# 数据处理
pyarrow>=7.0.0   # 用于读取 Parquet 文件
scikit-learn>=1.0.0  # 用于 K-Means 聚类

# 可视化
matplotlib>=3.4.0
seaborn>=0.11.0

# 仿真 (Algorithm 4: DES)
simpy>=4.0.0

# Jupyter (可选，用于交互式分析)
jupyter>=1.0.0
ipykernel>=6.0.0
